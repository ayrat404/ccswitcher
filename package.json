{
  "name": "ccswitcher",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code account switcher tray app (frontend assets)",
  "scripts": {
    "build": "node -e \"require('fs').cpSync('src','dist',{recursive:true})\"",
    "dev": "node -e \"require('fs').cpSync('src','dist',{recursive:true})\""
  }
}
