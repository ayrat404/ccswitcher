{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the settings window and tray.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
